server {
    listen 80;
    server_name {{ server_name }};

    location / {
        include uwsgi_params;
        uwsgi_pass unix:{{ base_dir }}/uwsgi.sock;
    }

    location /static {
        alias {{ site_dir }}/static;
    }

}
